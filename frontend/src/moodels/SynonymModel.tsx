/**
 * Interface representing a model for synonyms.
 *
 * @interface SynonymModel
 * @property {string[]} synonyms - An array of strings where each string is a synonym.
 */
export interface SynonymModel {
  synonyms: string[];
}
